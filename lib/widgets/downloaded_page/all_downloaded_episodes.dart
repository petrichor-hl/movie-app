import 'package:flutter/material.dart';

class ListAllEpisode extends StatelessWidget {
  const ListAllEpisode({super.key});

  @override
  Widget build(BuildContext context) {
    // print('all_downloaded_episodes build');
    return Container(
      height: 500,
      width: double.infinity,
      color: Colors.deepPurple,
    );
  }
}
